<tr>
    <th scope="row" width="10%" ><%= producto_nuevo.id %></th>
    
        <td><%= link_to producto_nuevo.nombre, productos_path(producto_nuevo), style:"color: black; text-decoration: none; " %></td>
        <td><%= producto_nuevo.precio %></td>
        <td><%= producto_nuevo.descripcion %></td>
        <td><%= producto_nuevo.cantidad %></td>
        <td><%= producto_nuevo.categoria.categoria %></td>
        <td><% producto_nuevo.imagenes.each do |imagen| %>
            <%= image_tag imagen %>
        <% end %></td>
    <td width="10%" ><%= link_to "Editar", editar_productos_path(producto_nuevo), style:"color: black;" %> | <%= link_to "Eliminar", eliminar_productos_path(producto_nuevo), method: :delete, data:{confirm: '¿Seguro que quieres eliminar?'}, style:"color: black;"  %></td>
</tr>